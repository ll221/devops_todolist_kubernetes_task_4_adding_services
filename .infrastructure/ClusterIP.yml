apiVersion: v1
kind: Service
metadata:
  name: todoapp-clusterip
  namespace: todoapp
spec:
  selector:
    app: todolist
  type: ClusterIP
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080